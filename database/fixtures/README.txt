RECYCLON SANITIZED FIXTURES

recyclon_schema.sql contains schema only. It intentionally contains no users,
passwords, addresses, GPS coordinates, sales, bookings, or production values.

Use the migration files in ../migrations for an existing legacy database.
Never copy the protected legacy dump or a production database into this folder.
